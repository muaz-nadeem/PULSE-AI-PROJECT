# Supabase Configuration
# Get these values from your Supabase project settings: https://app.supabase.com/project/_/settings/api

NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase Service Role Key (Server-side only, for cron jobs)
# IMPORTANT: Never expose this key to the client!
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# ============================================
# AI Configuration (Gemini)
# ============================================

# Google Gemini API Key
# Get this from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key

# Enable/disable AI features (set to "true" to enable)
NEXT_PUBLIC_AI_FEATURES_ENABLED=true

# Enable daily automated AI schedule generation (cron job)
AI_DAILY_BRAIN_ENABLED=true

# Enable AI Coach chat feature
AI_COACH_ENABLED=true

# ============================================
# Vercel Cron Configuration
# ============================================

# Secret for authenticating cron job requests
# Generate a secure random string: openssl rand -hex 32
CRON_SECRET=your_cron_secret_here

# ============================================
# Instructions
# ============================================

# 1. Copy this file to .env.local (it's already in .gitignore)
# 2. Replace the placeholder values with your actual credentials
#
# Supabase Setup:
#   - Go to Project Settings > API in your Supabase dashboard
#   - Copy "Project URL" for NEXT_PUBLIC_SUPABASE_URL
#   - Copy "anon public" key for NEXT_PUBLIC_SUPABASE_ANON_KEY
#   - Copy "service_role" key for SUPABASE_SERVICE_ROLE_KEY (keep this SECRET!)
#
# Gemini API Setup:
#   - Visit https://makersuite.google.com/app/apikey
#   - Create a new API key
#   - Copy it to GEMINI_API_KEY
#
# Vercel Cron Setup:
#   - Generate a secure CRON_SECRET using: openssl rand -hex 32
#   - Add this same secret to your Vercel project environment variables
#   - The cron job is configured in vercel.json to run daily at 5:00 AM UTC

# Gemini AI Configuration (for AI Daily Planner)
# Get your API key from: https://makersuite.google.com/app/apikey

NEXT_PUBLIC_GEMINI_API_KEY=your_gemini_api_key_here

# Instructions:
# 1. Go to https://makersuite.google.com/app/apikey
# 2. Sign in with your Google account
# 3. Click "Create API Key"
# 4. Copy the API key and paste it above
# 5. The free tier includes 60 requests per minute


#   GEMINI_API_KEY=your_gemini_api_key
#   SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
#   CRON_SECRET=your_secure_random_string
#   NEXT_PUBLIC_AI_FEATURES_ENABLED=true
#   AI_DAILY_BRAIN_ENABLED=true
#   AI_COACH_ENABLED=true

#   -- Execute 002_ai_system.sql in Supabase SQL Editor